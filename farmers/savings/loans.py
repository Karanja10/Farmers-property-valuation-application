from analysis import Analysis
from users import User


class Loans(object):
	"""docstring for ClassName"""
	def __init__(self, transaction_history, loan_limit, usage, status, period, transaction _id, transaction_volumeIn,transaction_volume_out):
		super(Users, self).__init__(first_name,last_name,phone,location,id_no,KRA_pin,email,password,pin)
		self.transaction_history = transaction_history
		self.loan_limit=loan_limit
		self.usage=usage
		self.status=['listed','good']
		self.period=period


		def history():
			for i in transaction_history:
				if self.transaction_volume_out >= self.transaction_volumeIn:
					print("Save more to get more")
				if self.transaction_volumeIn > self.transaction_volume_out:
					print("Proceed with loan application")
		def status():
			if self.status=="listed"
			print("You had defaulted another loan, transact more to improve eligibility")
			if self.status=="good"
			print("Congratulations! You are qualified, Proceed to take the loan")
		def eligibility():
			while self.status=="good" and loan is None:
				if analysis_results ="eligible":
					print(f"You are eligible for a loan of: {eligibility.getAmountValid()}")
					print(f"Posible loan schedules for your target amount:{schedules.getLoanSchedule()}")
		